# version: '3.8'
services:
  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - DEV_HTTPS=false
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0 --port 3000"
    expose:
      - "3000"
